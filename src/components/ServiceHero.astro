---
import { Image } from 'astro:assets';

interface Props {
  title: string;
  subtitle: string;
  imageSrc?: ImageMetadata;
  imageAlt: string;
}

const { title, subtitle, imageSrc, imageAlt } = Astro.props;
---

<section class="relative overflow-hidden z-0 m-0">
  <!-- Hero Image - Mobilde header'ın hemen altında, tam genişlik -->
  {imageSrc && (
    <div class="w-full md:hidden m-0">
      <div class="aspect-video bg-dark-lighter relative">
        <Image
          src={imageSrc}
          alt={imageAlt}
          class="absolute inset-0 w-full h-full object-cover"
          loading="eager"
          fetchpriority="high"
          quality={75}
          aria-hidden="true"
        />
        <div class="absolute inset-0 bg-gradient-to-t from-dark via-dark/50 to-transparent"></div>
      </div>
    </div>
  )}

  <!-- Background with vignette effect - Desktop için -->
  <div class="hidden md:flex md:items-center absolute inset-0 bg-gradient-to-br from-dark via-dark-light to-dark min-h-[70vh]">
    {imageSrc ? (
      <Image
        src={imageSrc}
        alt={imageAlt}
        class="absolute inset-0 w-full h-full object-cover opacity-30"
        loading="eager"
        fetchpriority="high"
        quality={75}
        aria-hidden="true"
      />
    ) : (
      <div class="absolute inset-0 bg-dark-lighter"></div>
    )}
    <div class="absolute inset-0 bg-gradient-to-t from-dark via-transparent to-transparent"></div>
    <div class="absolute inset-0 bg-gradient-to-r from-dark/80 via-transparent to-dark/60"></div>
  </div>

  <!-- Content Container -->
  <div class="container mx-auto px-4 pt-0 pb-8 md:py-20 relative">
    <div class="max-w-4xl mx-auto text-center">
      <h1 class="text-3xl sm:text-4xl md:text-5xl lg:text-6xl font-bold mb-4 md:mb-6 leading-tight text-white">
        {title}
      </h1>
      <p class="text-lg sm:text-xl md:text-2xl text-gray-300 mb-6 md:mb-8 leading-relaxed">
        {subtitle}
      </p>
      <div class="hidden md:flex flex-col sm:flex-row gap-4 justify-center">
        <a
          href="tel:+905326584722"
          class="bg-primary text-dark px-8 py-4 rounded-md font-bold text-lg hover:bg-primary-dark transition-colors text-center shadow-lg"
        >
          📞 Hemen Ara
        </a>
        <a
          href="https://wa.me/905326584722?text=Merhaba, turbo hizmeti hakkında bilgi almak istiyorum."
          target="_blank"
          rel="noopener noreferrer"
          class="bg-whatsapp text-white px-8 py-4 rounded-md font-bold text-lg hover:opacity-90 transition-opacity text-center shadow-lg"
        >
          💬 WhatsApp'tan Yaz
        </a>
      </div>
    </div>
  </div>
</section>

